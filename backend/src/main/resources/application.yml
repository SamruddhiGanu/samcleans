spring:
  application:
    name: storage-health-ranker
  datasource:
    url: jdbc:sqlite:${user.home}/.storage-health/database_v2.db
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 30000
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.community.dialect.SQLiteDialect
        jdbc:
          batch_size: 100
        order_inserts: true
        order_updates: true

logging:
  level:
    com.storagehealth: DEBUG
    org.springframework: WARN
    org.hibernate: WARN
  file:
    name: ${user.home}/.storage-health/logs/app.log
    max-size: 10MB
    max-history: 10

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
