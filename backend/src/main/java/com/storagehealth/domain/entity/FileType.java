package com.storagehealth.domain.entity;

/**
 * Enum representing the broad category of a file.
 * Used for semantic ranking and display grouping.
 */
public enum FileType {
    IMAGE,
    VIDEO,
    DOCUMENT,
    ARCHIVE,
    EXECUTABLE,
    MEDIA,
    TEMPORARY,
    OTHER
}
